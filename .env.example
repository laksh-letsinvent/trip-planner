# Deploy-time config for docker-compose.yml (auto-loaded from this file
# if you copy it to `.env` next to docker-compose.yml on the VM).
# GROQ_API_KEY / SERPER_API_KEY live in crew/.env instead — see
# crew/.env.example — not duplicated here.

# The public URL the app is served at. Used for the API's CORS allow-list.
WEB_ORIGIN=https://fam-trip.letsinvent.co.uk
